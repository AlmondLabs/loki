/** Filled in by Vite's `define` (app/vite.config.ts) from package.json. */
declare const __LOKI_VERSION__: string;
/** "owner/name" on GitHub, from package.json `repository`; the update check asks its releases. Empty when unset. */
declare const __LOKI_REPO__: string;
